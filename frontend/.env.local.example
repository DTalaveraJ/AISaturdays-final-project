# Map provider: "leaflet" (OpenStreetMap, no key needed) or "google" (requires key below)
NEXT_PUBLIC_MAPS_PROVIDER=leaflet

# Google Maps API key (only needed when NEXT_PUBLIC_MAPS_PROVIDER=google)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_key
